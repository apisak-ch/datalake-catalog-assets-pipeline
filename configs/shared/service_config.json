{
  "services": [
    {
      "kind": "messaging",
      "name": "kafka",
      "description": "Manual messaging service for tracking Kafka topics, no live connector"
    },
    {
      "kind": "database",
      "name": "hbase",
      "description": "Manual database service for tracking HBase tables, no live connector"
    },
    {
      "kind": "pipeline",
      "name": "k8s-application",
      "description": "Manual pipeline service for tracking online/streaming k8s consumer apps, no live connector"
    },
    {
      "kind": "pipeline",
      "name": "airflow",
      "description": "Manual pipeline service for tracking batch Airflow DAGs, no live connector"
    },
    {
      "kind": "storage",
      "name": "hdfs",
      "description": "Manual storage service for tracking HDFS files/zones, no live connector"
    },
    {
      "kind": "database",
      "name": "hive",
      "description": "Manual database service for tracking Hive tables, no live connector"
    },
    {
      "kind": "storage",
      "name": "ftp-mymo",
      "description": "Manual storage service for the MyMo-dedicated source FTP server (/data/prod/file/mymo/ToLAKE/), where MyMo/vipera drops daily export files for the lake to collect. No live connector"
    },
    {
      "kind": "storage",
      "name": "ftp-k8s",
      "description": "Manual storage service for tracking files delivered to downstream k8s-hosted FTP processors (mymo_processor, secure_plus_processor, ...), no live connector"
    },
    {
      "kind": "storage",
      "name": "ftp-go-anywhere",
      "description": "Manual storage service for the GoAnywhere MFT source FTP server (/gsbmymo/), a DV (data vendor) drop location shared across multiple source files (e.g. MYMO_DEPOSIT, MYMO_LOAN), no live connector"
    }
  ]
}
